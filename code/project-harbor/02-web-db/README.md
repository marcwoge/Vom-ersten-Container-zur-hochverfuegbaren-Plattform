# Stufe 02 – Web + DB + Adminer
```bash
cp .env.example .env        # Werte ersetzen!
docker compose up -d
docker compose --profile debug up -d   # Adminer auf :8081
docker compose ps
docker compose down
```
